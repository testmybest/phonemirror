pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
        // JitPack for webrtc-sdk
        maven { url "https://jitpack.io" }
    }
}
rootProject.name = "PhoneMirror"
include ':app'
